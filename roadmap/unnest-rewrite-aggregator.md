---
id: R182
title: "Retire legacy reactor and unnest graphitron-rewrite to repo root"
status: In Progress
bucket: structural
theme: legacy-migration
depends-on: []
created: 2026-05-19
last-updated: 2026-07-01
---

# Retire legacy reactor and unnest graphitron-rewrite to repo root

Carved out from R26's "Retire legacy + unnest the rewrite aggregator" sub-item so the repo-topology change has its own Spec and review trail. R26 closed 2026-07-01 with every other bullet under it shipped; this item is now the sole remaining carrier of that closing step and no longer depends on R26 as an open tracker.

Today the rewrite reactor lives nested under `graphitron-rewrite/`; the repo root still hosts the legacy `graphitron-parent` reactor (`graphitron-codegen-parent`, `graphitron-common`, `graphitron-example`, `graphitron-maven-plugin`, `graphitron-schema-transform`, `graphitron-servlet-parent`). This is operationally load-bearing in surprising ways: GitHub's `release: created` event always loads workflow files from the default branch, so any release tag, on any branch, runs the `main`-branch `maven-publish.yml`, which builds the root reactor (legacy). A `v10.0.0` tag cut from a rewrite-branch commit would republish the legacy artifacts at 10.0.0, not the rewrite reactor. The rewrite-branch publish workflow that knows about `-f graphitron-rewrite/pom.xml` and accepts `-RC<n>` suffixes is unreachable from a release event. Unnesting collapses the surface: one root reactor, one publish workflow on `main`, no `-f` flag, no nested-vs-root ambiguity.

## Trigger condition (gating)

Every legacy consumer must be migrated to the new plugin before this lands; cadence is dictated by per-consumer feature work. Promoting prematurely strands consumers on a deleted reactor.

**Gate cleared (2026-06-30):** the migration milestone has been reached and the user has signed off on retiring the legacy reactor. The item moves into the active pipeline.

## End state

- `graphitron-rewrite/`'s aggregator POM becomes the repo root POM.
- Modules relocate up one level: `graphitron-javapoet`, `graphitron`, `graphitron-fixtures-codegen`, `graphitron-sakila-db`, `graphitron-sakila-service`, `graphitron-mcp`, `graphitron-jakarta-rest`, `graphitron-maven-plugin`, `graphitron-sakila-example`, `graphitron-lsp`, `roadmap-tool` (the full `<modules>` list in `graphitron-rewrite/pom.xml`, corrected from an earlier draft that named a nonexistent `graphitron-maven` and omitted `graphitron-mcp` / `graphitron-jakarta-rest`). `graphitron-tree-sitter-natives` moves too (it's a plain subdirectory, not a reactor module ; no `<parent>`/`relativePath` fix needed) but its hardcoded path shows up in CI, see step 6.
- The legacy `graphitron-parent` reactor is gone, so the sole remaining parent (`graphitron-rewrite-parent`) is promoted to the root POM; no two-way POM merge is needed once the legacy root is deleted first.
- The duplicated `graphitron-javapoet` becomes the only copy (the legacy copy is already gone after the delete commit).
- `.github/workflows/maven-publish.yml` on `main` drops the `-f graphitron-rewrite/pom.xml` flag and adopts the RC-aware tag regex from the rewrite-branch workflow. `rewrite-build.yml`, `preview-docs.yml`, and any path filters lose their `graphitron-rewrite/` prefixes.
- **Documentation restructure: `graphitron-rewrite/docs/` becomes a Diataxis-shaped `docs/architecture/` inside the site module ; not a same-named collision to dodge, a shape problem to fix.** Repo-root `docs/` (the `graphitron-docs` site module) already publishes `docs/manual/` as an explicit Diataxis tree (`tutorial/`, `how-to/`, `reference/`, `explanation/`); `graphitron-rewrite/docs/` is today a flat, non-Diataxis 11-file dump that `docs/pom.xml`'s `stage-architecture` execution copies wholesale to the public `/architecture/` URL, with `README.adoc` renamed to `index.adoc` on the way (antrun, ~line 153). A full file-by-file content read (not a filename scan) found almost no actual overlap between the two trees ; they already cross-reference each other by design (nine `docs/manual/**` pages name a specific `architecture/*` counterpart as "the contributor-facing detail", e.g. `classifier-mental-model.adoc` → `code-generation-triggers.adoc`). The fix is reshaping the destination tree into the same quadrant structure `docs/manual/` already has, folding it in as first-class site content (`${project.basedir}/architecture`) instead of a resource pulled from a sibling `../graphitron-rewrite/docs`, while fixing two content defects the read surfaced along the way: `workflow.adoc` is pure roadmap-internal process bookkeeping currently leaking onto the public site against its own stated policy, and `getting-started.adoc` conflates consumer-facing and contributor-facing content in one file. Full disposition table and build-config changes are in the dedicated Concrete-steps commit below; this bullet is the summary.
- `CLAUDE.md` "Scope" rule (legacy modules out-of-scope) is deleted; the whole repo is in scope again.
- `verify-standalone-build.sh` either retires or repurposes (no legacy artifacts exist to leak).

## Concrete steps

Three commits, not one. The reactor deletion, the unwrap, and the docs restructure are separated so each gives `git log --follow` a clean boundary and a docs-only revert doesn't touch reactor topology; we are **not** routing this through an R19-style history squash.

1. Confirm gating: every legacy consumer migrated, signed off by user. **Cleared 2026-06-30.**
2. Discard R19 (`history-squash.md`) before or alongside landing this item. R19's front-matter still reads `status: Ready`, which is inconsistent with this spec's "R19 is abandoned" claim ; a `Ready` item that a `Spec` item explicitly supersedes shouldn't stay `Ready`. R19's own numbers are also stale (April-2026 commit counts against a merge base that's since moved thousands of commits). Flip R19's status to `Discarded` (delete `graphitron-rewrite/roadmap/history-squash.md`) once this item's approach is confirmed as the accepted path, per the 2026-07-01 staleness audit's recommendation for R19.
3. **Commit 1 — delete the legacy reactor.** `git rm -r` the six legacy modules (`graphitron-codegen-parent`, `graphitron-common`, `graphitron-example`, `graphitron-maven-plugin`, `graphitron-servlet-parent`, `graphitron-schema-transform`) and the root `pom.xml` (its `<modules>` list is exactly those six). Delete `.github/workflows/maven-build.yml` in the same commit: it runs `mvn --file pom.xml` against the legacy root reactor on every push/PR to `main` and would fail the required `build` check the moment the root POM is gone (`rewrite-build.yml` already covers the rewrite reactor). `graphitron-rewrite/` stays nested and `docs/` stays put; both still build via `-f graphitron-rewrite/pom.xml`.
4. **Commit 2 — unwrap.** `git mv` the contents of `graphitron-rewrite/` up to the repo root *except* `graphitron-rewrite/docs/`, which Commit 3 handles on its own; promote `graphitron-rewrite-parent` to the root POM (fix `relativePath`s; no two-way merge needed since the legacy parent is already deleted); the surviving `graphitron-javapoet` is now the only copy. Keep this commit isolated.
5. **Commit 3 — restructure `graphitron-rewrite/docs/` into `docs/architecture/`, Diataxis-shaped.** Disposition, file by file (verified by full-content read, not filename matching):
   - `git mv graphitron-rewrite/docs/rewrite-design-principles.adoc docs/architecture/explanation/rewrite-design-principles.adoc`; same explanation-quadrant move for `dispatch-axes.adoc` and `typed-rejection.adoc`.
   - `git mv graphitron-rewrite/docs/code-generation-triggers.adoc docs/architecture/reference/code-generation-triggers.adoc`; same reference-quadrant move for `argument-resolution.adoc` and `runtime-extension-points.adoc`.
   - `git mv graphitron-rewrite/docs/testing.adoc docs/architecture/how-to/testing.adoc`; `git mv graphitron-rewrite/docs/tree-sitter-natives-release.adoc docs/architecture/how-to/release-natives.adoc`.
   - `README.adoc` → `docs/architecture/index.adoc`, reworked into a quadrant-picker landing page mirroring `docs/manual/index.adoc`'s framing; its module table becomes `docs/architecture/reference/modules.adoc` and its pipeline-diagram narrative becomes `docs/architecture/explanation/pipeline-overview.adoc` rather than living on the landing page (the publishing-policy paragraph can stay on the index, it's short).
   - `getting-started.adoc` splits along its own existing "How this is wired (for contributors)" subsection markers, which already tell consumers "you can skip this": the consumer-facing runtime-integration content (hello world, custom scalars, federation usage, tenant-scoped `DSLContext`, JWT context args, customizer safe surface) folds into the existing `docs/manual/how-to/` pages that already stub or cross-reference it (`custom-scalars.adoc`, `apollo-federation.adoc`, `tenant-scoping.adoc`, `dev-loop.adoc`); the contributor subsections plus the dev-loop internals (the JVM/LSP/MCP component breakdown) become `docs/architecture/how-to/dev-loop-internals.adoc`. Because `getting-started.adoc` is dissolved rather than moved to one quadrant path, its two *inbound-linked anchors* need explicit new homes so their xrefs can be repointed (not just the file path swapped): `#dev-loop-detail` (the "Dev loop" section) and `#native-runtime-dependency` (the "Native runtime dependency: there isn't one" tree-sitter-natives note, bidirectionally cross-linked with `docs/manual/reference/lsp-requirements.adoc`) both land in `dev-loop-internals.adoc`, carrying their anchor IDs so `#dev-loop-detail` / `#native-runtime-dependency` fragments resolve against the new file. Inbound xrefs that repoint accordingly are enumerated in the audit bullet below.
   - `workflow.adoc` does **not** join `docs/architecture/` at all: `git mv graphitron-rewrite/docs/workflow.adoc graphitron-rewrite/roadmap/workflow.adoc` (post-unnest: `roadmap/workflow.adoc`), co-located with the roadmap items and tooling it governs rather than the public docs site. It is pure roadmap-internal process bookkeeping (`R<n>` IDs, session-ID reviewer gates, Backlog/Spec/Ready states) that violates its own "User-facing-doc check" paragraph by being on the site at all today (`graphitron.sikt.no/architecture/workflow.html`); moving it out of the `docs/architecture/` tree is what stops it from being swept into the build, no explicit exclude filter needed. `roadmap-tool`'s item scanner (`Main.java:394`, `.filter(p -> p.getFileName().toString().endsWith(".md"))`) is confirmed `.md`-scoped, so the `.adoc` file sitting alongside the roadmap items is safely ignored; regenerate the README after the move as a sanity check anyway. Update every reference to the old path (`CLAUDE.md`, `.claude/skills/roadmap/SKILL.md`, `.claude/skills/srp/SKILL.md`, `.claude/skills/reviewer-prompt/SKILL.md`, `.claude/agents/principles-architect.md`, `README.md`, and this repo's own workflow references) to `roadmap/workflow.adoc`.
   - `docs/pom.xml`: fold `architecture/**/*.adoc` into the existing `stage-adoc` execution's `<includes>` (it already lists `manual/**/*.adoc` the same way, now that `architecture/` is local content under `${project.basedir}` rather than a resource pulled from `../graphitron-rewrite/docs`), and delete the now-unneeded `stage-architecture` execution and the antrun `README.adoc` → `index.adoc` copy hack (the new source file is already named `index.adoc`). Add `staging/architecture/{how-to,reference,explanation}/css` antrun copy targets, mirroring the existing `staging/manual/{tutorial,how-to,reference,explanation}/css` pattern.
   - `docs/index.adoc`: rewrite the "Architecture" row's description to match the "Diataxis-shaped; pick the quadrant..." framing already used for the "User Manual" row; the URL segment (`/architecture/`) and row title stay unchanged, so no other inbound link breaks.
   - Audit every existing `architecture/...` xref across `docs/**` and repoint each. The full inbound set (verified 2026-07-01, `grep -rn "architecture/" docs/ --include=*.adoc`) is eleven files: (a) xrefs to sub-files that moved into quadrant subdirectories, repointed at the new quadrant path, in `docs/manual/explanation/index.adoc`, `classifier-mental-model.adoc`, `how-it-works.adoc`, `docs/manual/how-to/dev-loop.adoc`, `docs/manual/how-to/test-your-schema.adoc`, and `docs/security.adoc` (the last → `architecture/reference/runtime-extension-points.adoc`); (b) xrefs into the *dissolved* `getting-started.adoc`, which repoint at the split destinations rather than a moved path, in `docs/faq.adoc` and `docs/quick-start.adoc` (plain `getting-started.adoc` refs → the relevant `docs/manual/how-to/` pages the consumer content folded into), `docs/manual/how-to/dev-loop.adoc` and `docs/manual/how-to/mcp-agent-context.adoc` (both `#dev-loop-detail` → `architecture/how-to/dev-loop-internals.adoc#dev-loop-detail`), and `docs/manual/reference/lsp-requirements.adoc` (`#native-runtime-dependency` → `architecture/how-to/dev-loop-internals.adoc#native-runtime-dependency`). `docs/index.adoc`'s row links to `architecture/index.adoc` itself and needs no change (handled by the `docs/index.adoc` bullet above).
   - `roadmap-tool` (`roadmap-tool/src/main/java/no/sikt/graphitron/roadmap/Main.java`) independently *generates* `architecture/...` cross-references that the `grep -rn "architecture/" docs/` audit above cannot see (it is Java in a sibling module, not an `.adoc` file), so the audit net has to widen to it:
     - README-header template (`Main.java` ~1211-1220): the hardcoded `../docs/code-generation-triggers.adoc`, `../docs/rewrite-design-principles.adoc`, and `../docs/workflow.adoc` markdown links that render into `roadmap/README.md`. Repoint the first two at their new quadrant paths (`../docs/architecture/reference/code-generation-triggers.adoc`, `../docs/architecture/explanation/rewrite-design-principles.adoc`); the workflow link becomes a bare sibling `workflow.adoc`, since this Commit co-locates `roadmap/workflow.adoc` next to the generated `roadmap/README.md`.
     - `mapAdocTarget` mapper (`Main.java:922`; the `../docs/<file>.adoc` arm is ~949-953): today it maps a `../docs/<file>.adoc` reference to a *flat* `../architecture/<file>.adoc` xref for the site-rendered roadmap. After the Diataxis move that flat path no longer resolves (files now live under `explanation/`, `reference/`, `how-to/`) and `workflow.adoc` leaves the site entirely. Make the mapper quadrant-aware (drive it from a slug -> quadrant table) and give `workflow.adoc` its own arm, now that it is a roadmap sibling not staged into `/architecture/`.
     - `renderAdocStatusBoard` status-board header (`Main.java` ~476-479): three *literal* `StringBuilder` appends, `xref:../architecture/code-generation-triggers.adoc`, `xref:../architecture/rewrite-design-principles.adoc`, and `xref:../architecture/workflow.adoc`, rendered into the site-rendered `/roadmap/` status-board page. These are static text, not routed through `mapAdocTarget`, so the mapper fix above does not reach them; they must be edited in place. Repoint the first two at their quadrant paths (`../architecture/reference/code-generation-triggers.adoc`, `../architecture/explanation/rewrite-design-principles.adoc`) and drop or replace the `xref:../architecture/workflow.adoc[Workflow]` sentence, since Commit 3 removes `workflow.adoc` from the site (a bare `../architecture/workflow.adoc` would be a permanent dead link on the live `/roadmap/` page). `grep -n "architecture/" Main.java` confirms the full generated set: 476/478/479 here plus the `:951` mapper (the `:426` hit is a javadoc comment, not a link).
     - Regenerate `roadmap/README.md` after the edits and confirm both the GitHub-rendered README and the staged `/roadmap/` site links (status board included) resolve. This is the same silent-broken-link failure mode the risks section flags for `docs/`, just outside the `docs/` grep net.
     - `docs/pom.xml`'s `render-roadmap-adoc` execution passes `${project.basedir}/../graphitron-rewrite/roadmap` as its scan-directory argument; drop the `graphitron-rewrite/` segment post-unnest (belongs with the step 6/7 path-prefix sweep, noted here because it feeds the same roadmap-render path).
   - `git rm -r` the now-empty `graphitron-rewrite/docs/` directory (or let `git mv` leave it empty and remove it explicitly, depending on which git version is in play).
6. Update workflows and CI to drop the `-f graphitron-rewrite/pom.xml` flag and `graphitron-rewrite/` path prefixes: `maven-publish.yml`, `rewrite-build.yml`, `preview-docs.yml`, `.gitlab-ci.yml`, and every workflow `paths:` filter. (`.gitlab-ci.yml` already targets the rewrite reactor, so this is a path-prefix edit, not a legacy-removal.) `preview-docs.yml`'s path filter simplifies further once Commit 3 lands: its separate `graphitron-rewrite/docs/**` line disappears entirely (the existing top-level `docs/**` line already covers the merged `docs/architecture/**`), leaving only the `roadmap/**` and `pom.xml` lines to re-point post-unnest. Also fix `.github/workflows/tree-sitter-natives-release.yml`: its `NATIVES_MODULE` and `GRAMMAR_DIR` env vars hardcode `graphitron-rewrite/graphitron-tree-sitter-natives/...`, which is not a `paths:` trigger filter and is easy to miss by grepping only for that key ; both need the `graphitron-rewrite/` prefix dropped or the release workflow silently breaks on the next natives release after the move.
7. Update docs and tooling paths: `CLAUDE.md` (scope section, common commands), `.claude/web-environment.md`, the `.claude/skills/*` and roadmap-tool path defaults (`graphitron-rewrite/roadmap` → `roadmap`). Also reword `rewrite-design-principles.adoc`'s "Rewrite builds independently of legacy Graphitron modules" section (now living at `docs/architecture/explanation/rewrite-design-principles.adoc`): it names `verify-standalone-build.sh` and asserts an invariant against pulling in legacy artifacts that no longer exist once commit 1 lands ; per "Documentation names only live tests/code", don't leave it asserting an invariant about a script or a legacy tree that no longer exists at the path it names.
8. Cut a release tag (`v10.0.0` or the next planned RC) to verify the consolidated publish workflow end-to-end against Maven Central staging. Note the release-event publish workflow is governed by `main`'s copy of `maven-publish.yml`, so the hazard only closes once this lands on `main`.

## Verified facts (2026-06-30)

- Root `pom.xml` `<modules>` is exactly the six legacy folders; it no longer lists `graphitron-rewrite` or `docs`.
- `docs/` already parents to `graphitron-rewrite-parent` and is wired in as `<module>../docs</module>` of the rewrite reactor, so deleting the root POM does **not** orphan it.
- `graphitron-rewrite/pom.xml` has no `<parent>` (standalone); no rewrite POM depends on any of the six legacy modules. The delete is safe for the rewrite build.
- `.github/workflows/maven-build.yml` is the legacy build (`mvn --file pom.xml`, JDK 21, matrix over `graphitron-example-server`); it must be deleted with commit 1.
- `docs/` (root) and `graphitron-rewrite/docs/` both contain a `README.adoc` today; `docs/pom.xml`'s `stage-architecture` execution and antrun copy step already treat them as distinct trees (see End state), confirming `graphitron-rewrite/docs/` needs restructuring into the site module, not a same-named move.
- Full-content read of both trees (all top-level `docs/*.adoc`, all `docs/manual/**/*.adoc`, all `graphitron-rewrite/docs/*.adoc`) found no stale or genuinely redundant content ; every apparent name-adjacent pair (`getting-started.adoc` vs. `quick-start.adoc`/tutorial, `rewrite-design-principles.adoc` vs. `graphitron-principles.adoc`, `README.adoc` vs. `index.adoc`) is a deliberately non-overlapping, cross-referenced pair. The one confirmed public-site policy violation is `workflow.adoc` (roadmap-internal process content shipping to `/architecture/workflow.html`); the one confirmed audience-mixing file is `getting-started.adoc`.

## Risks and mitigations

- **Git history navigation.** Renaming the directory and merging POMs muddies `git log -- <path>` for old paths. Mitigation: do the move in a single dedicated commit so a `git log --follow` boundary is clear, and document the pre-move SHA in `changelog.md`.
- **Consumer migration incomplete.** Mitigated by the gating condition above; do not begin work until user confirms.
- **CI path filters.** Workflows that trigger only on `graphitron-rewrite/**` will silently stop firing if the path filter isn't updated; audit all workflow `paths:` keys before merging.
- **Publish-workflow regression.** The `main` workflow on the day of the move must accept both the legacy `v<MAJOR>.<MINOR>.<PATCH>` and the rewrite `-RC<n>` form; test against a staging tag before the first real release.
- **Dead cross-references after the docs restructure.** Eleven files across `docs/**` (seven under `docs/manual/`, four at the `docs/` root; verified inbound set enumerated in the Commit 3 audit bullet above) hardcode an `architecture/...` xref; every one except `docs/index.adoc`'s link to `architecture/index.adoc` points at a file Commit 3 either moves into a quadrant subdirectory or dissolves (the `getting-started.adoc` anchors), so a missed one renders as a broken link on the live site, not a build failure (AsciiDoctor doesn't fail the build on an unresolved xref by default). The dissolved-file anchors (`#dev-loop-detail`, `#native-runtime-dependency`) are the highest-risk repoints because the target file changes *and* the anchor must survive the move. The `docs/` grep also misses `roadmap-tool`'s `Main.java`, which *generates* `architecture/...` references at three sites (README header template, the `mapAdocTarget` flat-path mapper, and the `renderAdocStatusBoard` status-board header); the Commit 3 roadmap-tool bullet handles all three, but the audit sweep must include `Main.java`, not just `docs/**/*.adoc`. Mitigation: after Commit 3, grep both `docs/` and `roadmap-tool/**/*.java` for `architecture/` references and diff against the disposition and audit bullets above; run `mvn -f graphitron-rewrite/pom.xml install -Plocal-db` (the `docs` profile renders by default) and click through the rendered `/architecture/` **and `/roadmap/`** sections under `docs/target/generated-docs/` before pushing.

## Closes

- R26's last open sub-item, "Retire legacy + unnest the rewrite aggregator". R26 itself was already closed (2026-07-01, see `changelog.md`) once every other bullet under it had shipped; this item carries that final step to completion independently.
- The `release`-event publish hazard: legacy artifacts can no longer be cut at 10.0.0 because there is no legacy reactor to build.
- R19 (`history-squash.md`), superseded outright: this item does not use the squash approach R19 describes. R19 discards (step 2 above) rather than shipping.
