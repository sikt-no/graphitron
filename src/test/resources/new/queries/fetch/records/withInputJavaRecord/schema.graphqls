type Query {
  customer(in: CustomerInput): Customer
  customerListed(in: [CustomerInput]): [Customer]
}

input CustomerInput @record(record: {name: "DUMMY_RECORD"}) {
  id: ID!
  id2: ID! @field(name: "ID", javaName: "otherID")
}

type Customer @table {
  id: ID!
}
