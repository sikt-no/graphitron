type Response {
  errors: [Union]
}

union Union = SomeError

type SomeError implements Error @error(handlers: [{handler: GENERIC, className: "java.lang.IllegalArgumentException"}]) {
  path: [String!]!
  message: String!
}
