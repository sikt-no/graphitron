type Query {
  query: Address @service(service: {name: "DUMMY_SERVICE"})
}

type Address @table {
  inner: InnerWrapper
  inner2: Wrapper2 # Double wrapper. Also checks for naming conflicts.
}

type Wrapper2 {
  inner: InnerWrapper
}

type InnerWrapper {
  postalCode: String!
}
