type Query {
  query(
    id: ID
    str: String
    bool: Boolean
    i: Int
    e: Enum
    in: DummyInput
    # date: Date # Can not handle this yet, need to make a better type mapping for fields.
    idList: [ID]
    inList: [DummyInput]
  ): DummyType

  queryNonNullable(
    id: ID!
    str: String!
    bool: Boolean!
    i: Int!
    e: Enum!
    in: DummyInput!
    # date: Date!
    idList: [ID!]!
    inList: [DummyInput!]!
  ): DummyType
}

enum Enum {
  A, B
}
