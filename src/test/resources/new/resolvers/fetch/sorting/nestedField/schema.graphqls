type Query {
  query(orderBy: Order @orderBy, first: Int = 100, after: String): CustomerConnection
}

enum OrderByField {
  NAME @index(name : "IDX_LAST_NAME")
}

type Customer @table {
  nested: Nested
}

type Nested {
  name: String @field(name: "LAST_NAME")
}
