type Wrapper {
  id: ID

  query(orderBy: Order @orderBy, first: Int = 100, after: String): CustomerConnection @splitQuery
}

enum OrderByField {
  NAME @index(name : "IDX_LAST_NAME")
}

type CustomerTable @table(name: "Customer") {
  name: String @field(name: "LAST_NAME")
}
