type Query {
  query(
    id: ID
    str: String
    bool: Boolean
    i: Int
    e: Enum
    in: Input
    # date: Date # Can not handle this yet, need to make a better type mapping for fields.
  ): Target

  queryNonNullable(
    id: ID!
    str: String!
    bool: Boolean!
    i: Int!
    e: Enum!
    in: Input!
    # date: Date!
  ): Target
}

type Target {
  id: ID
}

input Input {
  i: Int
}

enum Enum {
  A, B
}
