type Query {
  film(releaseYear: Int! @column(name : "RELEASE_YEAR")): Film
}

type Film @table {
  id: ID!
  rating: Rating!
  length: Int
}

enum Rating {
  G @mapEnum(name: "G"),
  PG_13 @mapEnum(name: "PG-13"),
  R @mapEnum(name: "R"),
}
