type Mutation {
  editCustomerAddress(in: EditAddressInput!): EditAddressResponse! @service(name: "TEST_CUSTOMER")
}

input EditAddressInput @record(table: "CUSTOMER") {
  id: ID!
  addressId: ID! @column(name: "ADDRESS_ID")
}

type EditAddressResponse {
  id: ID!
  addressId: ID!
}
