package no.fellesstudentsystem.graphql.resolvers.mutation;

import graphql.schema.DataFetchingEnvironment;
import java.lang.Exception;
import java.lang.Override;
import java.lang.String;
import java.util.concurrent.CompletableFuture;
import javax.inject.Inject;
import no.fellesstudentsystem.graphitron.services.TestPersonService;
import no.fellesstudentsystem.graphql.generated.api.EndrePersonSimpleMutationResolver;
import no.fellesstudentsystem.graphql.helpers.selection.SelectionSet;
import org.jooq.DSLContext;

public class EndrePersonSimpleGeneratedResolver implements EndrePersonSimpleMutationResolver {
    @Inject
    DSLContext ctx;

    @Override
    public CompletableFuture<String> endrePersonSimple(String id, DataFetchingEnvironment env)
            throws Exception {
        var testPersonService = new TestPersonService(ctx);
        var select = new SelectionSet(env.getSelectionSet());

        var endrePersonSimpleResult = testPersonService.endrePersonSimple(id);

        return CompletableFuture.completedFuture(endrePersonSimpleResult);
    }
}
