input In {
  name: [String]
}

type Film implements Node @table {
  id: ID!
  languages(s: [String] @field(name: "NAME")): [Language!]! @splitQuery @reference(key: "FILM__FILM_LANGUAGE_ID_FKEY")
  languagesInput(s: In): [Language!]! @splitQuery @reference(key: "FILM__FILM_LANGUAGE_ID_FKEY")
}

type Language implements Node @table {
  id: ID!
}

type Query {
  film: Film @notGenerated
}
