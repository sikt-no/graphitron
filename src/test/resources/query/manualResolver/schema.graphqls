type Query {
  customers(storeIds: [ID!]! @field(name: "STORE_ID")): [Customer]
}

type Customer implements Node @table {
  id: ID!
  address: Address! @splitQuery @notGenerated
}

type Address implements Node @table {
  id: ID!
  postalCode: String!
}
