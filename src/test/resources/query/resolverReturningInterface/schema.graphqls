type Query {
  query(id: ID!): Customer @notGenerated
}

type Customer implements Node @table {
  id: ID!
  address: Address!
  innerNode(id: ID!): Node! @splitQuery
}

type Address implements Node @table {
  id: ID!
}
