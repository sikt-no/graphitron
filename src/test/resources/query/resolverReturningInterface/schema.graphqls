type Kull implements Node @node {
  id: ID!
}

interface Node {
  id: ID!
}

type ProgramStudierett implements Node @node(table : "STUDIERETT") {
  id: ID!
  kull: Kull!
  referertNode(id: ID!): Node! @resolver
}

type Query {
  programStudierett(id: ID!): ProgramStudierett
}
