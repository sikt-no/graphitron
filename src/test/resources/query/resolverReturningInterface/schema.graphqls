type Kull implements Node @table {
  id: ID!
}

interface Node {
  id: ID!
}

type ProgramStudierett implements Node @table(name : "STUDIERETT") {
  id: ID!
  kull: Kull!
  referertNode(id: ID!): Node! @splitQuery
}

type Query {
  programStudierett(id: ID!): ProgramStudierett
}
