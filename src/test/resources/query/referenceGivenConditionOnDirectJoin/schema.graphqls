type Query {
  customer(id: ID!): Customer
}

type Customer @table {
  id: ID!
  historicalAddresses: [Address!]! @splitQuery @reference(condition : "TEST_CUSTOMER_ADDRESS")
}

type Address @table {
  id: ID!
}
