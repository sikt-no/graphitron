type Customer @table {
  id: ID!
}

type Address @table {
  id: ID!
  customers: [Customer!]! @splitQuery @reference(references: [{key : "CUSTOMER__CUSTOMER_ADDRESS_ID_FKEY"}])
}

type Query {
  address: Address @notGenerated
}
