type Query {
  customer: Customer @notGenerated
}

type Customer @table {
  id: ID!
  historicalAddresses: [Address!]! @splitQuery @reference(references: [{condition : {name: "TEST_CUSTOMER_ADDRESS", method: "customerAddress"}}])
}

type Address @table {
  id: ID!
  address: String
}
