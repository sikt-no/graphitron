type Query {
  #via tables starting from the referring type (PAYMENT): PAYMENT -> RENTAL -> INVENTORY -> FILM
  payment(id: ID!, title: String! @reference(via: [{table: "RENTAL"}, {table: "INVENTORY"}], table: "FILM")): Payment
}

type Payment @table {
  id: ID!
}
