type Query {
    rom(bygningskode: String!): Rom
}

type Rom @node(table: "ROM") {
    id: ID!
    romId: ID!
}

enum Kjonn @mapEnum(name: "KJONN_TEST") { #should NOT trigger warning
    MANN,
    KVINNE
}

enum Kjonn2 @mapEnum(name: "KJONN_TEST2") { #should trigger warning
    MANN,
    KVINNE
}
