type Query {
    customer(id: ID!): Film
}

type Film @table {
    id: ID!
 }

enum Rating @mapEnum(name: "RATING") {
    G,
    PG,
    R,
}

enum Rating2 @mapEnum(name: "UNKOWN_ENUM") {
    G,
    PG,
    R,
}