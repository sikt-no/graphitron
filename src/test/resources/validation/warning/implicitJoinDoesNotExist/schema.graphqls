type Query {
  store(id: ID!): Store
}

type Store @table {
  id: ID!
  city: City! @reference(references: [{table: "CUSTOMER"}, {table: "ADDRESS"}])
  payment: Payment!
}

type City @table {
  id: ID!
}

type Payment @table {
  id: ID!
}
