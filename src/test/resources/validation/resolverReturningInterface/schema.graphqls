type Query {
  customer(id: ID!): Customer
}

type Customer implements Node @table {
  id: ID!
  address: Address!
  nodeRef(id: ID!): Node! @splitQuery
}

type Address implements Node @table {
  id: ID!
}
