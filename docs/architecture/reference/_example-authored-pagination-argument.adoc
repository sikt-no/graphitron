// Generated from the corpus. Do not edit; CorpusFragmentTest writes it.
[source,graphql]
----
type Query {
  "A carrier whose author declared first themselves. The mint stands down on that name and appends only after, so the declaration survives as written."
  pagedFilms(
  first: Int): [Film!]! @asConnection @defaultOrder(primaryKey: true)
}

type Film @table(name: "film") {
  title: String
}
----

.What the pipeline makes of it
[cols="1,1,2"]
|===
| Coordinate | Verdict | Emitted

| `Film.title`
| `TABLE_COLUMN`, inferred
| `fetchers.FilmFetchers#title(Object)`

| `Query.pagedFilms`
| no claiming directive
| `fetchers.QueryFetchers#pagedFilms(DataFetchingEnvironment)`

| `QueryPagedFilmsConnection.edges`
| no claiming directive
| `fetchers.QueryPagedFilmsConnectionFetchers#edges(DataFetchingEnvironment)`

| `QueryPagedFilmsConnectionEdge.node`
| no claiming directive
| `fetchers.QueryPagedFilmsConnectionEdgeFetchers#node(DataFetchingEnvironment)`

|===
