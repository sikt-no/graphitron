// Generated from the corpus. Do not edit; CorpusFragmentTest writes it.
[source,graphql]
----
type Query {
  inventory: Inventory
  language: Language
}

type Language @table(name: "language") {
  "Discriminated participants, read from one table."
  mediaList: [MediaItem!]! @reference(path: [{key : "film_language_id_fkey"}])
}

interface MediaItem @table(name: "film") @discriminate(on: "text_rating") {
  title: String
}

type Inventory @table(name: "inventory") {
  "The same interface at single cardinality."
  media: MediaItem
}
----

.What the pipeline makes of it
[cols="1,1,2"]
|===
| Coordinate | Verdict | Emitted

| `Inventory.media`
| no claiming directive
| `fetchers.InventoryFetchers#media(DataFetchingEnvironment)`

| `Language.mediaList`
| no claiming directive
| `fetchers.LanguageFetchers#mediaList(DataFetchingEnvironment)`

| `MediaItem.title`
| `TABLE_COLUMN`, inferred
| no method of its own

| `Query.inventory`
| no claiming directive
| `fetchers.QueryFetchers#inventory(DataFetchingEnvironment)`

| `Query.language`
| no claiming directive
| `fetchers.QueryFetchers#language(DataFetchingEnvironment)`

|===
