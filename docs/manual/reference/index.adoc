= Reference
:description: Authoritative descriptions of every directive, plugin parameter, and runtime API entry point.
:!toc:

Authoritative descriptions of every directive, every Maven plugin parameter, every runtime API entry point, and every diagnostic code Graphitron emits.

== Runtime API

* xref:runtime-api.adoc[Runtime API reference]: the `Graphitron.buildSchema(...)` facade (single-arg and federation two-arg overloads), the `Graphitron.newExecutionInput(...)` schema-driven factory (`DSLContext` first, then typed `contextArgument` slots reflected from your `@service` / `@condition` directives), the sealed `GraphitronContext` per-request interface (`getDslContext`, `getContextArgument`, `getValidator`), and the `NodeIdEncoder` utility (generic `encode`/`peekTypeId`/`decodeValues` plus per-Node `encode<TypeName>` and `decode<TypeName>` helpers).

== Maven plugin

* xref:mojo-configuration.adoc[Maven plugin configuration]: every parameter on every goal of `graphitron-maven-plugin`. Covers the three goals (`generate`, `validate`, `dev`), the shared `<configuration>` parameters (`outputDirectory`, `outputPackage`, `jooqPackage`, `schemaInputs`, `namedReferences`), the `dev`-only watch parameters (`port`, `debounceMs`, `skipInitial`), and the two complex POM bindings (`<schemaInput>`, `<namedReference>`).

== Editor support

* xref:lsp-requirements.adoc[LSP requirements]: the LSP's native runtime. Both the GraphQL grammar and the `libtree-sitter` runtime ship bundled, so there is no system dependency to install. Covers the supported host architectures and the startup diagnostics.
* xref:lsp-inlay-hints.adoc[LSP inlay hints and classification hover]: the three editor-side config keys (`graphitron.inlayHints.inferredDirectives`, `graphitron.inlayHints.classification`, `graphitron.hover.classification`) that surface Graphitron's inference and classification layers inside the editor. Default off; opt in per key.

== Diagnostics

* xref:diagnostics-glossary.adoc[Diagnostics glossary]: every closed-set diagnostic code the validator emits, organised by category. Covers the three top-level rejection kinds (`author-error`, `invalid-schema`, `deferred`), the nine name-lookup attempt kinds (`column`, `table`, `foreign-key`, `service-method`, `lifter-method`, `enum-constant`, `type-name`, `nodeid-key-column`, `dml-kind`), and the four deferred emit-block reasons.

== Deprecations

* xref:deprecations.adoc[Deprecations]: aggregator for every deprecated directive argument, input field, and whole directive in the rewrite, with migration guidance pointing back at the canonical directive page.

== Directives

[cols="1,1",frame=none,grid=none]
|===
a|
=== Alphabetical

* xref:directives/asConnection.adoc[`@asConnection`]
* xref:directives/condition.adoc[`@condition`]
* xref:directives/defaultOrder.adoc[`@defaultOrder`]
* xref:directives/discriminate.adoc[`@discriminate`]
* xref:directives/discriminator.adoc[`@discriminator`]
* xref:directives/enum.adoc[`@enum`]
* xref:directives/error.adoc[`@error`]
* xref:directives/externalField.adoc[`@externalField`]
* xref:directives/field.adoc[`@field`]
* xref:directives/index-directive.adoc[`@index`] _(deprecated, use `@order(index:)`)_
* xref:directives/lookupKey.adoc[`@lookupKey`]
* xref:directives/multitableReference.adoc[`@multitableReference`] _(rejected, remove from the schema)_
* xref:directives/mutation.adoc[`@mutation`]
* xref:directives/node.adoc[`@node`]
* xref:directives/nodeId.adoc[`@nodeId`]
* xref:directives/notGenerated.adoc[`@notGenerated`] _(rejected, remove from the schema)_
* xref:directives/order.adoc[`@order`]
* xref:directives/orderBy.adoc[`@orderBy`]
* xref:directives/record.adoc[`@record`] _(deprecated, ignored, remove from the schema)_
* xref:directives/reference.adoc[`@reference`]
* xref:directives/referenceFor.adoc[`@referenceFor`]
* xref:directives/service.adoc[`@service`]
* xref:directives/splitQuery.adoc[`@splitQuery`]
* xref:directives/table.adoc[`@table`]

a|
=== By category

*Mapping* +
xref:directives/table.adoc[`@table`], xref:directives/field.adoc[`@field`], xref:directives/enum.adoc[`@enum`], xref:directives/record.adoc[`@record`] (deprecated, ignored)

*Joining* +
xref:directives/reference.adoc[`@reference`]

*Querying* +
xref:directives/asConnection.adoc[`@asConnection`], xref:directives/condition.adoc[`@condition`], xref:directives/externalField.adoc[`@externalField`], xref:directives/lookupKey.adoc[`@lookupKey`], xref:directives/splitQuery.adoc[`@splitQuery`]

*Mutating* +
xref:directives/mutation.adoc[`@mutation`], xref:directives/service.adoc[`@service`]

*Sorting* +
xref:directives/orderBy.adoc[`@orderBy`], xref:directives/order.adoc[`@order`], xref:directives/defaultOrder.adoc[`@defaultOrder`], xref:directives/index-directive.adoc[`@index`] (deprecated)

*Error handling* +
xref:directives/error.adoc[`@error`]

*Polymorphism* +
xref:directives/discriminate.adoc[`@discriminate`], xref:directives/discriminator.adoc[`@discriminator`]

*Global ID* +
xref:directives/node.adoc[`@node`], xref:directives/nodeId.adoc[`@nodeId`]

*Rejected by the rewrite* +
xref:directives/notGenerated.adoc[`@notGenerated`], xref:directives/multitableReference.adoc[`@multitableReference`] (remove from the schema)

|===

xref:../index.adoc[← Back to the manual landing]
