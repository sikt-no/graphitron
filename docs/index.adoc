= Graphitron
:description: Graphitron is a Java code generator that turns GraphQL schemas + jOOQ database models into resolvers.
:keywords: graphitron, sikt, graphql, jooq, java, code generation
:!toc:

[.hero]
== A code generator from GraphQL to jOOQ

Graphitron generates the wiring between a GraphQL schema and a jOOQ-modelled database, so that the resolvers, fetchers, and SQL come out of the schema rather than being maintained by hand.

[.advantage-box]
NOTE: This site is being rebuilt. Authoritative documentation is moving here from the in-repo `docs/` folders and from the previous Docusaurus site at https://graphitron.sikt.no/[graphitron.sikt.no]. Expect rapid iteration during the migration.

== Where things are

[cols="1,3", options="header"]
|===
| Section
| Contents

| Vision and principles
| The problem Graphitron solves and the design philosophy behind it. (Coming soon: rendered from `/docs/`.)

| Architecture
| Rewrite-internal documentation: model, classification taxonomy, runtime extension points. (Coming soon: rendered from `/graphitron-rewrite/docs/`.)

| Roadmap
| Per-status board of in-flight and shipped work. (Coming soon: rendered from `/graphitron-rewrite/roadmap/`.)
|===

== Source

Graphitron is developed by https://sikt.no/[Sikt] and lives at https://github.com/sikt-no/graphitron[github.com/sikt-no/graphitron].

The current build is `{graphitron-version}`.
