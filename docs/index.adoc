= Graphitron
:description: Graphitron is a Java code generator that turns GraphQL schemas + jOOQ database models into resolvers.
:keywords: graphitron, sikt, graphql, jooq, java, code generation
:!toc:
:noheader:

++++
<section class="hero-band">
  <h1 class="hero-title">Graphitron</h1>
  <p class="hero-tagline">A code generator for GraphQL APIs backed by jOOQ.</p>
  <div class="cta-row">
    <a class="cta-button" href="quick-start.html">Quick start</a>
    <a class="cta-button" href="https://github.com/sikt-no/graphitron/tree/claude/graphitron-rewrite" rel="noopener">GitHub</a>
  </div>
</section>
++++

Graphitron generates the wiring between a GraphQL schema and a jOOQ-modelled database, so resolvers, fetchers, and SQL come out of the schema rather than being maintained by hand.

// Decorative hero illustrations: emitted as HTML passthroughs so they carry
// aria-hidden="true". AsciiDoctor's image: macro does not support arbitrary
// HTML attributes, and alt="" alone is not enough; VoiceOver and a few other
// screen readers fall back to reading the file path when an empty-alt image
// has no aria-hidden, which is the failure mode being fixed here.
[.featurerow,cols="1a,1a,1a",frame=none,grid=none]
|===
|
+++<img class="decorative" src="images/Person2.svg" alt="" aria-hidden="true">+++

[.feature]
== Database-first

A well-designed database outlives every application built on top of it. Graphitron consumes jOOQ-generated types and treats the database schema as the source of truth, rather than abstracting it away.

xref:graphitron-principles.adoc[Read the principles]

|
+++<img class="decorative" src="images/Person3.svg" alt="" aria-hidden="true">+++

[.feature]
== Built on jOOQ and graphql-java

jOOQ provides the typed table classes that Graphitron uses to build SQL the compiler can check. graphql-java provides the schema-execution layer the generated resolvers plug into. Both are foundational, not incidental.

xref:dependencies.adoc[Why these dependencies]

|
+++<img class="decorative" src="images/Personer.svg" alt="" aria-hidden="true">+++

[.feature]
== Authorization stays in the database

Generated resolvers contain no authorization logic. PostgreSQL row-level security and per-request session variables enforce access rules where the rules already live, and where every other application that touches the data sees them too.

xref:security.adoc[Security model]
|===

== Documentation

[cols="1,3", options="header"]
|===
| Section
| Contents

| xref:quick-start.adoc[Quick Start]
| Prerequisites, jOOQ generation, GraphQL schema with directives, build wiring. Start here if you want to use Graphitron in a project.

| xref:manual/index.adoc[User Manual]
| Tutorial, how-to recipes, directive reference, and explanation of the design. Diataxis-shaped; pick the quadrant that matches what you are trying to do. Currently scaffold-only while pages are authored under roadmap item `R68`.

| xref:faq.adoc[FAQ]
| Purpose, how it works, licensing, production use, and where to ask for help.

| xref:vision-and-goal.adoc[Vision and Goal]
| What problem Graphitron solves and how it approaches the solution.

| xref:graphitron-principles.adoc[Graphitron Principles]
| The design philosophy behind Graphitron, the long-term thinking behind the architectural decisions.

| xref:dependencies.adoc[Dependencies]
| Why jOOQ and GraphQL-Java are foundational, and what that means for you.

| xref:security.adoc[Security]
| Why we enforce security at the database layer.

| xref:architecture/index.adoc[Architecture]
| Rewrite-internal documentation: code generation triggers, sealed model hierarchy, runtime extension points, design principles, contributor workflow.

| xref:roadmap/index.adoc[Roadmap]
| Active and Backlog work, plus a xref:roadmap/by-theme.adoc[by-theme view] and the xref:roadmap/changelog.adoc[changelog]. Plans are authored as markdown next to the code; the rendered view here updates on every push.
|===

The current build is `{graphitron-version}`.
