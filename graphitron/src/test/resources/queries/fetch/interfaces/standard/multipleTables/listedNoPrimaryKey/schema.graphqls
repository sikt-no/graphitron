type Query {
    query: [SomeInterface]
}

interface SomeInterface {
    umid: String
}

type PgUserMapping implements SomeInterface @table(name: "PG_USER_MAPPING") {
    umid: String @field(name: "UMID")
}
