type Query {
  query: Wrapper
}

type Wrapper {
  customer(countryId: String! @field(name: "COUNTRY_ID") @reference(references: [{condition: {name: "REFERENCE_CUSTOMER_CONDITION", method: "cityCustomer"}}])): CustomerTable
}
