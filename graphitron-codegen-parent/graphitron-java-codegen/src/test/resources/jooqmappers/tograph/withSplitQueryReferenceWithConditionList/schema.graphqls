type Query {
  query: Customer @service(service: {name: "DUMMY_SERVICE"})
}

type Customer @table {
  payments: [Payment] @splitQuery @reference(path: [{condition: {className: "no.sikt.graphitron.codereferences.conditions.ReferenceCustomerCondition"}}])
}

type Payment @table {
  id: ID!
}