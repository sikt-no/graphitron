type Query {
  customerQuery(in: [String!]!): CustomerPayload @service(service: {name: "MAPPER_FETCH_SERVICE"})
}

type CustomerPayload {
  customers: [Customer!]! @splitQuery
}

type Customer @table {
  id: ID!
}