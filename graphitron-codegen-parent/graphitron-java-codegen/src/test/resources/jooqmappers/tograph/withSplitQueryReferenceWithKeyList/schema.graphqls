type Query {
  query: Customer @service(service: {name: "DUMMY_SERVICE"})
}

type Customer @table {
  payments: [Payment] @splitQuery @reference(path: [{key: "PAYMENT__PAYMENT_CUSTOMER_ID_FKEY"}])
}

type Payment @table {
  id: ID!
}