type Query {
    test(input: TestInput): String @service(service: {className: "no.sikt.graphitron.codereferences.services.TypeMismatchService", method: "check"})
}

input TestInput @record(record: {className: "no.sikt.graphitron.codereferences.records.CustomerJavaRecord"}) {
    someID: String
}
