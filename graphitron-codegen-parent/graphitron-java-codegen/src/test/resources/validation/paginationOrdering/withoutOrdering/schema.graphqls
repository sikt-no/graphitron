type Query {
    # FILM_LIST is a view without primary key
    # This should fail validation because it's paginated without @defaultOrder or @orderBy
    filmLists(first: Int, after: String): FilmListConnection
}

type FilmListConnection {
    edges: [FilmListEdge]
    pageInfo: PageInfo
}

type FilmListEdge {
    cursor: String
    node: FilmList
}

type FilmList @table(name: "FILM_LIST") {
    title: String
    description: String
    category: String
}
