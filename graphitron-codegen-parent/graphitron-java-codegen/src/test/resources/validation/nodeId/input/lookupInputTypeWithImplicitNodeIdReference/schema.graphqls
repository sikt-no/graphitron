type Query {
  query(input: LookupInput @lookupKey): [Address]
}

type Address @table {
  cityId: Int @field(name: "CITY_ID")
}

input LookupInput {
  customerId: [ID!]! @nodeId(typeName: "CustomerNode")
}