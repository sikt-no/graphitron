type Store @table {
  buddy: Customer @experimental_procedureCall(
    procedure: "inventory_held_by_customer",
    arguments: "p_inventory_id: STORE_ID",
    target: "heldBy"
  )
}

type Customer @table {
  heldBy: ID
}
