type Query {
  someInventory(inventoryId: ID): Customer @experimental_procedureCall(
    procedure: "inventory_held_by_customer",
    arguments: "p_inventory_id: inventoryId",
    target: "heldBy"
  )
}

type Customer @table {
  heldBy: ID @field(name: "CUSTOMER_ID")
}
