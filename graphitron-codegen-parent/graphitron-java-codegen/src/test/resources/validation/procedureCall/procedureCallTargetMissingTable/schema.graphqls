type Query {
  someInventory(inventoryId: ID): Untabled @experimental_procedureCall(
    procedure: "inventory_held_by_customer",
    arguments: "p_inventory_id: inventoryId",
    target: "heldBy"
  )
}

type Untabled {
  heldBy: ID
}
