interface SomeInterface {
  id: ID
}

type Other implements SomeInterface @table(name: "ACTOR") {
  id: ID @field(name: "ACTOR_ID")
}

type Customer @table {
  held: SomeInterface @experimental_procedureCall(procedure: "inventory_held_by_customer", arguments: "p_inventory_id: CUSTOMER_ID")
}
