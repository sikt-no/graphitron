type Mutation {
    mutation(in: CustomerNodeInputTable!): Customer @mutation(typeName: DELETE)
}

type Customer implements Node @node @table {
    id: ID!
    address: Address
}

type Address @table {
    id: ID!
}