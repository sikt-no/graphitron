type Mutation {
    mutation(in: CustomerNodeInputTable): VacationDestination @mutation(typeName: DELETE)
}

type VacationDestination @table(name: "VACATION_DESTINATION") {
    destination: Int @field(name: "DESTINATION_ID")
}