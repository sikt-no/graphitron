type Mutation {
    mutation(in: CustomerInput!): ID! @mutation(typeName: DELETE) @nodeId(typeName: "CustomerNode")
}

input CustomerInput @table(name: "CUSTOMER") {
    id: ID! @field(name: "ADDRESS_ID")
}
