type Actor @table {
  id: ID @field(name: "ACTOR_ID")
}

union SomeUnion = Actor

type Customer @table {
  held: SomeUnion @experimental_procedureCall(procedure: "inventory_held_by_customer", arguments: "p_inventory_id: CUSTOMER_ID")
}
