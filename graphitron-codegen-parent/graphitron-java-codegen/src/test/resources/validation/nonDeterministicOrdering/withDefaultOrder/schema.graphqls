type Query {
    # FILM_LIST is a view without primary key
    # With @defaultOrder — should NOT produce a non-deterministic ordering warning
    filmLists: [FilmList] @defaultOrder(index: "idx_title")
}

type FilmList @table(name: "FILM_LIST") {
    title: String
    description: String
    category: String
}
