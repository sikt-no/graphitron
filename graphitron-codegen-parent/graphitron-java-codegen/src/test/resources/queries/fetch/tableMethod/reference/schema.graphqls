type Query {
    customer(first_name: String): Customer @tableMethod(service: {className: "no.sikt.graphitron.codereferences.services.CustomerTableMethod", method: "customerTable"})
}

type Customer @table{
    id: ID
    staff: Staff @reference(references: [{table: "Address"}]) @tableMethod(service: {className: "no.sikt.graphitron.codereferences.services.StaffTableMethod", method: "staffTable"})
}

type Staff @table{
    id: ID
}