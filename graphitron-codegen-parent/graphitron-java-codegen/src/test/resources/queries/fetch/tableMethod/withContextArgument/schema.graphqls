type Query {
    customer: Customer @tableMethod(service: {className: "no.sikt.graphitron.codereferences.services.CustomerTableMethod", method: "customerTable"}, contextArguments: "ctxField")
}

type Customer @table{
    id: ID
}