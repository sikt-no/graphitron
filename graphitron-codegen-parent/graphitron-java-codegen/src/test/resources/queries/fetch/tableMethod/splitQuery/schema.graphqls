type Query {
    customer(first_name:String): Customer @tableMethod(service: {className: "no.sikt.graphitron.codereferences.services.CustomerTableMethod", method: "customerTable"}) @splitQuery
}

type Customer @table {
    id: String
}
