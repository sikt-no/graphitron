type Query {
    customer: Customer @tableMethod(service: {className: "no.sikt.graphitron.codereferences.services.CustomerTableMethod", method: "customerTable"})
}

type Customer @table {
    address: Address @splitQuery
}

type Address @table {
    id: ID
}