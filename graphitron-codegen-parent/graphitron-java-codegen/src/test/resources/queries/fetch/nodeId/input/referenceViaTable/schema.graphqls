type Query {
  query(cityId: ID! @nodeId(typeName: "City") @reference(path: [{table: "ADDRESS"}])): Customer
}

type Customer implements Node @node @table {
  id: ID! @nodeId
}

type City implements Node @node @table {
  id: ID! @nodeId
}
