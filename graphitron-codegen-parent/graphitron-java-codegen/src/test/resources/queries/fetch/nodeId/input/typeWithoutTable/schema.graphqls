type Query {
  query(in: In!): CustomerNoTable
}

input In @table(name: "CUSTOMER") {
  id: ID! @nodeId(typeName: "CustomerNode")
}

type CustomerNoTable {
  id: ID! @nodeId(typeName: "CustomerNode")
}
