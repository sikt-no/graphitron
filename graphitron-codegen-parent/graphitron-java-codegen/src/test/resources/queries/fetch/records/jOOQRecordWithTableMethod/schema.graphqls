type Query {
    customer(in: CustomerInput): CustomerTable @tableMethod(service: {className: "no.sikt.graphitron.codereferences.services.CustomerTableMethod", method: "customerTable"})
}

input CustomerInput @table(name: "CUSTOMER") {
    id: ID!
    first: String! @field(name: "FIRST_NAME")
}
