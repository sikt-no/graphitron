type Query {
    customer: Customer @tableService(service: {className: "no.sikt.graphitron.codereferences.services.CustomerTableService", method: "customerTable"})
}

type Customer @table {
    id: String
}