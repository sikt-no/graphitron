type Query {
  store: Store
}

type Store @table {
  id: ID
  customer(inventoryId: ID): Customer @experimental_procedureCall(target: "heldBy", procedure: "inventory_held_by_customer", arguments: "p_inventory_id: inventoryId")
}

type Customer @table {
  id: ID
  heldBy: Int
}
