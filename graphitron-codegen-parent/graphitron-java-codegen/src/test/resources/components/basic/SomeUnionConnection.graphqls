type SomeUnionConnection {
    edges: [SomeUnionConnectionEdge]
    pageInfo: PageInfo
    nodes: [SomeUnion!]!
    totalCount: Int
}

type SomeUnionConnectionEdge {
    cursor: String
    node: SomeUnion
}
