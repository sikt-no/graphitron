type Query {
    query: Payload @service(service: {className: "MapperFetchService", method: "cityQuery"})
}

type Payload {
    cities: [City]
}

type City @table {
    addresses: [Address] @splitQuery
}

type Address @table {
    id: ID
}
