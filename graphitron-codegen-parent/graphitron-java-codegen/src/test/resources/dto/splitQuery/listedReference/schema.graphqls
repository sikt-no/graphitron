type Query {
    vacation: Vacation @notGenerated
}
type Vacation @table(name: "VACATION") {
    destination: [VacationDestination] @splitQuery
}

type VacationDestination @table(name: "VACATION_DESTINATION") {
    someString: String
}
