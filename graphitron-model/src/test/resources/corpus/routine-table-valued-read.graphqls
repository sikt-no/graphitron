type Tilgang @table(name: "tilganger_for_feidebruker_med_fs_fiktivt_fnr") {
  organisasjonskode: Int
  rollekode: String
}
extend type Query {
  "A table-valued database routine, read like a table."
  tilganger(env: String!, serviceId: String!, feideId: String!): [Tilgang!]!
    @routine(name: "tilganger_for_feidebruker_med_fs_fiktivt_fnr", argMapping: "pEnv: env, pServiceId: serviceId, pFeideId: feideId")
    @defaultOrder(fields: [{name: "organisasjonskode"}, {name: "rollekode"}])
    @classified(source: Query, operations: [OrderBy, Select], target: List, targetShape: Table)

}

extend schema @expectEquals(relation: "intent_resolved_field_claim", rows: """
  type_name,    field_name,        classifier,   tier
  CorpusAnchor, name,              TABLE_COLUMN, INFERRED
  Query,        tilganger,         ROUTINE,      AUTHORED
  Tilgang,      organisasjonskode, TABLE_COLUMN, INFERRED
  Tilgang,      rollekode,         TABLE_COLUMN, INFERRED
  """)

extend schema @expectEquals(relation: "intent_authored_field_claim", rows: """
  type_name, field_name, classifier, trigger, decoded
  Query,     tilganger,  ROUTINE,    routine, TRUE
  """)

extend schema @expectEquals(relation: "intent_bound_table", rows: """
  type_name,    table_name
  CorpusAnchor, category
  Tilgang,      tilganger_for_feidebruker_med_fs_fiktivt_fnr
  """)

extend schema @expectEquals(relation: "plan_launcher_command", rows: """
  type_name, field_name,   source,       result
  Query,     corpusAnchor, AnchorTable,  SingleRecord
  Query,     tilganger,    RoutineChain, RecordList
  """)

{
  tilganger(env: "test", serviceId: "svc", feideId: "user@example.org") {
    organisasjonskode
    rollekode
  }
}
