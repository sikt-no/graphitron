type Query {
  someType0(
    param: String!,
  ): [SomeType!]! @asConnection(connectionName: "SomeTypeConnection")

  someType1(param: String!): [SomeType]
}

type SomeType implements Node {
  id: ID!
  otherType: [OtherType!]! @asConnection(defaultFirstValue: 10, connectionName: "OtherTypeConnection") @splitQuery
}

type OtherType {
  field: String!
}
