type Query {
  someType0(
    param: String!,
    first: Int = 100,
    after: String,
  ): [SomeType] @asConnection

  someType1(
    param: String!,
    first: Int = 100,
    after: String,
  ): [SomeType]! @asConnection
}

type SomeType implements Node {
  id: ID!
}
