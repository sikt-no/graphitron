directive @directiveWithRequiredParam(param: String!) on FIELD_DEFINITION

type Query {
    someType: SomeType
}

type SomeType {
    fieldWithDirective: String! @directiveWithRequiredParam # @directiveWithRequiredParam missing the required 'param' parameter.
    otherField: ID!
}
