{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": ".claude/scripts/session-start-web-env.sh"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": ".claude/scripts/wait-for-web-env.sh",
            "timeout": 2700
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git push *)",
            "command": "cmd=$(jq -r '.tool_input.command'); case \"$cmd\" in *'git push'*) ;; *) exit 0;; esac; case \"$cmd\" in *claude/graphitron-rewrite*) exit 0;; esac; printf '%s\\n' '{\"hookSpecificOutput\":{\"hookEventName\":\"PostToolUse\",\"additionalContext\":\"Trunk fast-forward not done. Follow up with the publish skill, or: git push origin HEAD:claude/graphitron-rewrite\"}}'"
          }
        ]
      }
    ]
  }
}
