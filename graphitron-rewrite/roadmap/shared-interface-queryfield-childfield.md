---
title: "Shared interface for `QueryField` / `ChildField` table-bound parallels"
status: Backlog
bucket: cleanup
priority: 4
---

# Shared interface for `QueryField` / `ChildField` table-bound parallels

Root variants drop `joinPath` but share `filters · orderBy · pagination`.
