---
title: "`Set<T>` parent-keys on `@service` methods"
status: Backlog
bucket: architecture
priority: 7
---

# `Set<T>` parent-keys on `@service` methods

Decide: require `List<T>` (predictable batching order, the current direction) or broaden `BatchKey`. One known offender (`navnAlleSprak`).
