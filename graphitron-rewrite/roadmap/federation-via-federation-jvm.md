---
title: "Apollo Federation via federation-jvm transform"
status: Backlog
bucket: architecture
priority: 5
---

# Apollo Federation via federation-jvm transform

Replace the `QueryEntityField` stub with a `GraphitronSchemaBuilder` post-step wrapping the Graphitron schema via `Federation.transform`; deletes the stub after migration.
