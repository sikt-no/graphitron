= Inference-axis coverage report
:description: Placeholder. The live data renders only in CI; this file in the git tree is a non-data stub.

This page is regenerated on every successful push to `claude/graphitron-rewrite` by the `Rewrite reactor CI` workflow, uploaded as a build artifact, and rendered onto the public doc site by the deploy workflow. The version that lives in the repository is a placeholder so local doc builds and PR previews still resolve the include; do not read data from it.

For the current report, see the rendered page on https://graphitron.sikt.no[graphitron.sikt.no].

To regenerate the report locally for inspection (overwrites this file in your working copy; do not commit the result):

[source,shell]
----
mvn -f graphitron-rewrite/pom.xml -pl roadmap-tool exec:java \
  -Dexec.args='leaf-coverage graphitron-rewrite' -Pleaf-coverage
----

The `-Pleaf-coverage` flag is the opt-in profile that lands with R133; until that ships, a bare `mvn verify` from the rewrite root is enough to populate the traces the regen reads from.
