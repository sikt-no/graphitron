---
title: "Audit custom pagination-arg-name support"
status: Backlog
bucket: architecture
priority: 10
---

# Audit custom pagination-arg-name support

Decide: remove `PaginationSpec` plumbing for non-default `first`/`after` names (likely dead code), or document and add an execution fixture.
