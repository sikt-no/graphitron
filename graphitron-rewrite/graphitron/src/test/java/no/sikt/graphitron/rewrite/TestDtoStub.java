package no.sikt.graphitron.rewrite;

/**
 * Minimal plain-class stub for testing that a non-{@link org.jooq.TableRecord} element type
 * in a service Sources parameter is rejected at classification time.
 */
class TestDtoStub {
    String id;
}
