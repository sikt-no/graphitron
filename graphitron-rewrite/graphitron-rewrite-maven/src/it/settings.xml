<?xml version="1.0" encoding="UTF-8"?>
<settings>
  <!--
    Settings for maven-invoker-plugin integration tests.
    The plugin JAR is installed via maven-install-plugin:install-file in
    pre-integration-test using the .flattened-pom.xml so the resolved version
    is used. All other artifacts must already be present in the local
    repository from a prior build.
  -->
</settings>
