on:
  push:
name: TOC Generator
jobs:
  generateTOC:
    name: TOC Generator
    runs-on: ubuntu-latest
    steps:
      - name: Generate TOC
        uses: technote-space/toc-generator@v4
        with:
          TARGET_PATHS: graphitron-codegen-parent/graphitron-java-codegen/README.md, graphitron-example/README.md, graphitron-schema-transform/README.md
