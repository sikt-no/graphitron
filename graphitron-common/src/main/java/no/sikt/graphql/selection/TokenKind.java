package no.sikt.graphql.selection;

enum TokenKind {
    NAME,
    STRING,
    INT,
    FLOAT,
    VARIABLE,
    LBRACE,
    RBRACE,
    LPAREN,
    RPAREN,
    LBRACKET,
    RBRACKET,
    COLON,
    EOF
}
