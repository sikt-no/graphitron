# Graphitron Enhancement Proposals

This folder contains proposed changes to Graphitron.
Proposals start out as `drafts`.
They can then transition to `accepted` or `discarded`.
An `accepted` proposal can be `implemented` or `superseded`.
