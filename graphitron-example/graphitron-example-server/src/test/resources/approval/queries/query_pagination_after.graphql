# after should return the next page of results after the cursor
{
    customers(after: "WzEwMF0", first: 2) {
        nodes {
            id
        }
        pageInfo {
            endCursor
            startCursor
        }
    }
}