mutation {
    updateCustomerEmail(input: {customerId: 1, email: "DOES_NOT_MATTER_RESPONSE_HARDCODED_IN_SERVICE"}) {
        emailUpdates {
            customerEmail {
                customerId
                email
            }
        }
    }
}
