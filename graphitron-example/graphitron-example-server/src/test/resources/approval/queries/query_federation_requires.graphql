query FederationRequires {
    _entities(representations: [
        {
        __typename: "FederatedCustomer",
        firstName: "DOROTHY",
        lastName: "TAYLOR"
        }
    ]) {
        ... on FederatedCustomer {
            id
            email
            externalEmail
            requiresEmail
        }
    }
}