# pageInfo returns correct information about the current (first) page of the query
{
    customers {
        pageInfo {
            endCursor
            hasNextPage
            hasPreviousPage
            startCursor
        }
    }
}