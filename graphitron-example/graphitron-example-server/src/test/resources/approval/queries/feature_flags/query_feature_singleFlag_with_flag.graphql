# activeBool requires @feature(flags: "experimental"), X-Feature-Flags: experimental - should succeed
query CustomerWithSingleFlagField {
    customers(first: 1) {
        nodes {
            id
            activeBool
        }
    }
}
