# storeId requires @feature(flags: ["experimental", "preview"]), X-Feature-Flags: experimental,preview - should succeed
query CustomerWithMultiFlagField {
    customers(first: 1) {
        nodes {
            id
            storeId
        }
    }
}
